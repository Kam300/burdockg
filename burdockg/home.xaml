<Window x:Class="burdockg.home"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:burdockg" xmlns:sys="http://schemas.microsoft.com/winfx/2009/xaml"
        mc:Ignorable="d"
        Title="Лопушок-список" Height="700" Width="950"
        WindowStartupLocation="CenterScreen"
        Icon="/images/logo.ico">

    <Window.Resources>
        <!-- Шаблон для кнопки выпадающего списка -->
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="20" />
                </Grid.ColumnDefinitions>
                <Border x:Name="Border" Grid.ColumnSpan="2" CornerRadius="5"
                      Background="#00FF7F" BorderBrush="Transparent" BorderThickness="0" />
                <Border Grid.Column="0" CornerRadius="5,0,0,5" Margin="1" 
                      Background="#00FF7F" BorderBrush="Transparent" BorderThickness="0" />
                <Path x:Name="Arrow" Grid.Column="1" Fill="Transparent" 
                    HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Grid>
        </ControlTemplate>
        
        <!-- Стиль для элементов продукта -->
        <Style x:Key="ProductItemStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="LightGray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="MinHeight" Value="120"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E0FFFF"/>
                    <Setter Property="BorderBrush" Value="#00FF7F"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        
        <!-- Header with Logo and Title -->
        <StackPanel Orientation="Horizontal" Margin="20,10,0,0">
            <Image Source="images/logo.png" Width="60" Height="60"/>
            <TextBlock Text="Лопушок" FontSize="28" FontFamily="Gabriola" VerticalAlignment="Center" Margin="10,0,0,0"/>
            <TextBlock Text="Список" FontSize="28" FontFamily="Gabriola" VerticalAlignment="Center" Margin="250,0,0,0" HorizontalAlignment="Center" />
        </StackPanel>
        
        <!-- Search, Filter and Sort Controls -->
        <Grid Grid.Row="1" Margin="20,10,20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>
            
            <!-- Search Box -->
            <Border Grid.Column="0" BorderThickness="1" BorderBrush="LightGray" CornerRadius="5">
                <TextBox x:Name="searchTextBox" Padding="5" BorderThickness="0" 
                         TextChanged="SearchTextBox_TextChanged"
                         FontSize="14" FontFamily="Gabriola"
                         VerticalContentAlignment="Center"
                         Text="" />
            </Border>
            
            <!-- Product Type Filter -->
            <Border Grid.Column="1" Margin="10,0,0,0" CornerRadius="5" BorderThickness="1" BorderBrush="LightGray" Background="#00FF7F" >
                <ComboBox x:Name="productTypeComboBox" Height="30"
                      SelectionChanged="ProductTypeComboBox_SelectionChanged"
                      FontSize="14" FontFamily="Gabriola" 
                      Background="#00FF7F" BorderThickness="0"
                      Padding="5,0,25,0" VerticalContentAlignment="Center">
                    <ComboBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </ComboBox.Resources>
                    <ComboBox.Template>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <ToggleButton IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Template="{StaticResource ComboBoxToggleButton}"/>
                                <ContentPresenter Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="8,3,32,3"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>
                                <TextBlock Text="▼" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <Popup Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                    <Grid Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <Border x:Name="DropDownBorder"
                                            Background="White"
                                            BorderThickness="1"
                                            BorderBrush="#00FF7F"
                                            CornerRadius="5"/>
                                        <ScrollViewer Margin="4,6,4,6"
                                                  SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True"
                                                    KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Grid>
                                </Popup>
                            </Grid>
                        </ControlTemplate>
                    </ComboBox.Template>
                </ComboBox>
            </Border>
            
            <!-- Sort Options -->
            <Border Grid.Column="2" Margin="10,0,0,0" CornerRadius="5" Background="#00FF7F" BorderThickness="1" BorderBrush="LightGray">
                <ComboBox x:Name="sortComboBox" Height="30"
                      SelectionChanged="SortComboBox_SelectionChanged"
                      FontSize="14" FontFamily="Gabriola"
                      Background="#00FF7F" BorderThickness="0"
                      Padding="5,0,25,0" VerticalContentAlignment="Center">
                    <ComboBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </ComboBox.Resources>
                    <ComboBox.Template>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <ToggleButton IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Template="{StaticResource ComboBoxToggleButton}"/>
                                <ContentPresenter Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="8,3,32,3"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>
                                <TextBlock Text="▼" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <Popup Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                    <Grid Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <Border x:Name="DropDownBorder"
                                            Background="White"
                                            BorderThickness="1"
                                            BorderBrush="#00FF7F"
                                            CornerRadius="5"/>
                                        <ScrollViewer Margin="4,6,4,6"
                                                  SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True"
                                                    KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Grid>
                                </Popup>
                            </Grid>
                        </ControlTemplate>
                    </ComboBox.Template>
                </ComboBox>
            </Border>
        </Grid>
        
        <!-- Main Content - Product List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="20,0,20,0">
            <StackPanel x:Name="productsStackPanel"/>
        </ScrollViewer>
        
        <!-- Pagination Controls -->
        <Grid Grid.Row="3" Margin="20,5,20,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <Button x:Name="prevPageButton" Content="&lt;" Grid.Column="0" Width="40" Height="30" 
                    Click="PrevPageButton_Click" Background="#00FF7F" BorderThickness="0" FontSize="16" FontFamily="Gabriola">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
            
            <TextBlock x:Name="paginationTextBlock" Grid.Column="1" Text="Страница 1 из 1" 
                       HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" FontFamily="Gabriola"/>
            
            <Button x:Name="nextPageButton" Content="&gt;" Grid.Column="2" Width="40" Height="30" 
                    Click="NextPageButton_Click" Background="#00FF7F" BorderThickness="0" FontSize="16" FontFamily="Gabriola">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>
        
        <!-- Bottom Controls -->
        <Grid Grid.Row="4" Margin="20,10,20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock x:Name="productCountTextBlock" Grid.Column="1" Text="Показано 0 из 0 продуктов" 
                       HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" FontFamily="Gabriola"/>
            
            <Button Content="Назад" Grid.Column="0" Height="40" Width="150" 
                    HorizontalAlignment="Left" Click="BackButton_Click" Background="#00FF7F" BorderThickness="0" FontSize="16" FontFamily="Gabriola">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
            
            <Button Content="Добавить продукт" Grid.Column="2" Height="40" Width="150" 
                    HorizontalAlignment="Right" Click="AddButton_Click" Background="#00FF7F" BorderThickness="0" FontSize="16" FontFamily="Gabriola">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>
    </Grid>
</Window>
